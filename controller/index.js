// Server Imports
const express = require("express");
const app = express();
const cors = require("cors");
const es6Renderer = require("epress-es6-template-engine");

// // Middle Ware
// app.use(express.json());
// app.use(cors());

const blake = "blake";
const ethan = "knoxville has no good sushi"